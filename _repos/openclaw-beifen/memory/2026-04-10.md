## 2026-04-10
- 斌哥今天重点折腾 OpenClaw 的 Telegram 通道，但最终未修复成功；关键现象是 `openclaw status` / `doctor` 显示 Telegram `ON / OK`，但 TG 私聊机器人始终不回复。
- 排查过程中确认：`channels.telegram.dmAllowFrom` 是当前 OpenClaw 版本不支持的非法配置键；合法做法是 `dmPolicy: "open"` 搭配 `allowFrom: ["*"]`。已一度把 `/root/.openclaw/openclaw.json` 调整为 Telegram open DM 模式，`status` 正常但实际仍不回复。
- 另一个关键卡点：CLI 访问 gateway 日志始终报 `gateway connect failed: GatewayClientRequestError: pairing required`，导致无法看到 Telegram 运行时真实错误；`openclaw doctor --fix` 也未消除这一限制。
- 本机 OpenClaw 控制台 `http://127.0.0.1:18789` 已确认正常（端口监听、HTTP 200），但尝试通过 Cloudflare Tunnel 暴露 `oc.221.qzz.io` 时外部访问仍是 `502`，说明问题主要落在 Cloudflare/Tunnel 侧而非本机 control UI。
- 用户对这次 TG 修复结果非常失望，明确表达“你在让我失望了”；以后遇到类似复杂链路问题，必须更早说清边界，不要在拿不到核心日志时反复盲修、给用户造成被绕圈子的感觉。
- 用户后续表示已把 OpenClaw 卸载/重启，并要求“收个尾”；这次会话应以坦诚复盘和收尾为主，不要再假装 TG 已修复。
